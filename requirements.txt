# Pinned to the OpenVINO 2026.3 stable release (2026-08-04) — the first release
# whose GenAI ships gemma-4 *unified* dispatch, so the nightly index and the
# from-source GenAI build this project needed for Gemma-4-12B are both retired.
# Install on Windows with Python 3.12:
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt

# OpenVINO stable stack
openvino==2026.3.0
openvino-genai==2026.3.0.0
openvino-tokenizers==2026.3.0.0
nncf==3.3.0

# Model download
huggingface_hub==1.17.0

# API server
fastapi==0.136.3
uvicorn==0.49.0
jinja2==3.1.6
pyyaml==6.0.3
